/**
 * @module src/utils
 * Barrel export do módulo utilitário.
 * Importar daqui em vez de referenciar arquivos internos diretamente:
 *
 * ```ts
 * import { formatDate } from '../utils';
 * ```
 */
export * from './date';
